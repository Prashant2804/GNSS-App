name: pi_ci
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python -m pip install -U pip
      - run: cd pi && pip install -e ".[dev]"
      - run: cd pi && pytest
